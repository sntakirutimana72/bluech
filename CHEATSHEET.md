```shell
  docker build -t bluech .

  docker run bluech

  docker exec -it b867053a2971 /bin/bash

  service postgresql start

  python3.9

  from app.utils.db_connect import db_connector
  from app.models import *
```